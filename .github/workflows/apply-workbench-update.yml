name: Apply AOG workbench update

on:
  push:
    branches: [main]
    paths:
      - '.sync_payload/**'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply verified workbench update
        shell: bash
        run: |
          set -euo pipefail
          for n in 00 01 02 03 04; do
            if [ ! -f ".sync_payload/workbench_update.part${n}" ]; then
              echo "Payload upload in progress: missing part${n}"
              exit 0
            fi
          done
          cat .sync_payload/workbench_update.part00 .sync_payload/workbench_update.part01 .sync_payload/workbench_update.part02 .sync_payload/workbench_update.part03 .sync_payload/workbench_update.part04 | base64 -d | xz -d > index_v3.0_minimal.html
          actual=$(sha256sum index_v3.0_minimal.html | awk '{print $1}')
          expected='49d708fb95f5073afd109c5d60062d116afb3277d6382e771d46a44678b83299'
          if [ "$actual" != "$expected" ]; then
            echo "SHA256 mismatch: $actual"
            exit 1
          fi
          rm -rf .sync_payload
          rm -f .github/workflows/apply-workbench-update.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Refactor AOG workbench flow'
          git push
